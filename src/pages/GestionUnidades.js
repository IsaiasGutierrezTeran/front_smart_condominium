// src/pages/GestionUnidades.js
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '../components/UI/Card';

const GestionUnidades = () => {
  return (
    <div className="p-6">
      <h1 className="text-3xl font-bold mb-6">Gestión de Unidades</h1>
      <Card>
        <CardContent>
          <p>Interfaz para gestionar unidades habitacionales (en desarrollo)</p>
        </CardContent>
      </Card>
    </div>
  );
};

export default GestionUnidades;